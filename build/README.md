# Build resources

Place optional packaging assets here for electron-builder.

## Icon (optional)

- Add `icon.ico` (Windows) when available.
- Until then, the packaged app uses Electron’s default icon.

No custom icon is shipped in Stage 12.
